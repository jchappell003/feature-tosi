{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Debug Locally",
      "program": "${workspaceFolder}/system/dist/index.js",
      "args": [
        "serve",
        "${workspaceFolder}/bake.yaml"
      ],
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}